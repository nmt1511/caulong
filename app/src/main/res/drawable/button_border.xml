<!-- res/drawable/button_border.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" /> <!-- Bên trong trong suốt -->
    <stroke android:width="1dp" android:color="@android:color/white" /> <!-- Viền trắng dày 2dp -->
    <corners android:radius="8dp" /> <!-- Bo góc 8dp -->
</shape>

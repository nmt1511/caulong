<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Caulong"
        tools:targetApi="31">
        <activity
            android:name=".user.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"/>
        <activity android:name=".booking.ServiceTypeListActivity"/>
        <activity android:name=".booking.ServiceListActivity"/>
        <activity android:name=".menubottom.Account"/>
        <activity android:name=".menubottom.AccountFragment"/>
        <activity android:name=".menubottom.HistoryFragment"/>
        <activity android:name=".menubottom.NotificationFragment"/>
        <activity android:name=".menuleft.AboutFragment"/>
        <activity android:name=".menuleft.ShareFragment"/>
        <activity android:name=".booking.Booking_yard" />
        <activity android:name=".booking.NuocActivity" />
        <activity android:name=".booking.FoodActivity" />
        <activity android:name=".booking.DichVuActivity" />
    </application>

</manifest>